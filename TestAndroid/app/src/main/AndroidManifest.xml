<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.testandroid.testandroid">

    <uses-permission android:name="android.permission.REQUEST_COMPANION_USE_DATA_IN_BACKGROUND" />
    <uses-permission android:name="android.permission.REQUEST_COMPANION_RUN_IN_BACKGROUND" />

    <application
        android:name=".FuckApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">

        <!-- 权限测试 -->
        <activity android:name=".NotPermissActivity">

        </activity>

        <!-- 架构测试 -->
        <activity
            android:name=".HiberActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="com.testandroid.testandroid.HiberActivity" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- A Activity -->
        <activity
            android:name=".AActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="com.testandroid.testandroid.AActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- B Activity -->
        <activity
            android:name=".BActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="com.testandroid.testandroid.BActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- C Activity -->
        <activity
            android:name=".CActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="com.testandroid.testandroid.CActivity" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>
